import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 10,
    },
    messageBox: {
        borderRadius: 20,
        padding: 10,
        marginBottom: 5,
    },
    name: {
        color: "#00BFFF",
        fontWeight: 'bold',
        marginBottom: 5,
    },
    message: {
        fontSize: 17,
        marginBottom: 8,
        marginTop: 5
    },
    time: {
    }
});

export default styles;